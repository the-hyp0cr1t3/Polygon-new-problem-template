#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int n;

inline int readAndCheckAnswer(InStream& in) {
    // int result = in.readInt(0, 1000000000);
    // long long result = in.readLong(0, 1000000000000000000);
    // in.quitf(_wa, "%d-th type not found", something);
    // return result;
}

int main(int argc, char * argv[])
{
    registerTestlibCmd(argc, argv);
    // n = inf.readInt();

    // int ja = readAndCheckAnswer(ans);
    // int pa = readAndCheckAnswer(ouf);
    
    // if (ja < pa)
        // quitf(_wa, "expected %I64d, found %I64d", ja, pa);
    
    // quitf(_ok, "answer is %I64d", ja);
}